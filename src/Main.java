import java.time.LocalDate;

public class Main {
    public static void main(String[] args) {

     CurrentCurrency currentCurrency=new CurrentCurrency();
    currentCurrency.readFile();
     currentCurrency.currentDate();
    // currentCurrency.currentCash();
     // currentCurrency.currentDate();

    }
}
